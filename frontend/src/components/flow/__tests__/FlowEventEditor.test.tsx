import { FlowEventEditor } from '@/components/flow/FlowEventEditor';

describe('FlowEventEditor', () => {
  it('exports a component', () => {
    expect(typeof FlowEventEditor).toBe('function');
  });
});
